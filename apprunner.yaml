version: 1.0
runtime: php81
build:
  commands:
    build:
      - chmod +x startup.sh
      - curl https://docs.aws.amazon.com/aws-sdk-php/v3/download/aws.phar --output aws.phar
run:
  command: ./startup.sh
  runtime-version: phpfpmtest
  network:
    port: 8080
    env: APP_PORT
